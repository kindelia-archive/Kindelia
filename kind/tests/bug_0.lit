name x
name NotBug

bond NotBug() : #word {
    let x : #word = #42
    let x : #word = x
    x
}

// Ok
eval {
    NotBug()
} : #word

// BUG
eval {
    let x : #word = #42
    let x : #word = x
    x
} : #word
