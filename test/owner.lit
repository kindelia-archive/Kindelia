file dec, inc @ count : #word = #1

bond inc() : & #word {
  get c = count
  set count = +(c, #1)
  return #1
}

bond dec() : & #word {
  get c = count
  set count = -(c, #1)
  return #2
}

eval {
  run inc()
  run inc()
  run dec()
  
  get c = count
  return c
} : & #word

save